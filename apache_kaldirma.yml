---
  - hosts: all
    become: true
    tasks:

    - name: Bu görev Ubuntu'dan apache ve kütüphane yüklemesini kaldırıyor.
      apt:
        name:
          - apache2
          - libapache2-mod-php
        state: absent
      when: ansible_distribution == "Ubuntu"
